BR2_xtensa=y
BR2_XTENSA_DYNCONFIG=y
BR2_TOOLCHAIN_EXTERNAL=y
BR2_TOOLCHAIN_EXTERNAL_PATH="/home/jcmvbkbc/ws/tensilica/crosstool/fdpic/builds/xtensa-esp32s3-linux-uclibcfdpic"
BR2_TOOLCHAIN_EXTERNAL_CUSTOM_PREFIX="$(ARCH)-esp32s3-linux-uclibcfdpic"
BR2_TOOLCHAIN_EXTERNAL_HEADERS_6_6=y
BR2_TOOLCHAIN_EXTERNAL_WCHAR=y
BR2_TOOLCHAIN_EXTERNAL_HAS_SSP=y
BR2_OPTIMIZE_S=y
# BR2_PIC_PIE is not set
BR2_SSP_NONE=y
BR2_RELRO_NONE=y
BR2_ROOTFS_OVERLAY="board/espressif/esp32s3/rootfs_overlay board/espressif/esp32s3/tdeck-pro/rootfs_overlay"
# setup-home.sh repoints root's home at /home/root. It has to run here, not
# from the overlay: buildroot regenerates /etc/passwd after the overlay is
# applied (it stamps the root password hash in), so an overlay copy of passwd
# would be overwritten. Leaving it out of this line is not a cosmetic loss --
# root lands in /root, on the read-only cramfs, instead of the writable jffs2.
# no-open-wifi.sh deletes the default wpa_supplicant.conf the buildroot
# package installs; it matches any open network and joins one on its own.
BR2_ROOTFS_POST_BUILD_SCRIPT="board/espressif/esp32s3/trim-libs.sh board/espressif/esp32s3/setup-home.sh board/espressif/esp32s3/no-open-wifi.sh board/espressif/esp32s3/trim-target.sh"
BR2_ROOTFS_POST_FAKEROOT_SCRIPT="board/espressif/esp32s3/mkfs-etc.sh"
BR2_ROOTFS_POST_IMAGE_SCRIPT="board/espressif/esp32s3/post-build.sh"
BR2_LINUX_KERNEL=y
BR2_LINUX_KERNEL_CUSTOM_GIT=y
BR2_LINUX_KERNEL_CUSTOM_REPO_URL="https://github.com/jcmvbkbc/linux-xtensa.git"
BR2_LINUX_KERNEL_CUSTOM_REPO_VERSION="xtensa-6.11-esp32-tag"
# The hardware RSA driver and the BLE provisioning pipe are not upstream.
# Their directories are untracked by the kernel's own git repo, so a clean
# re-extraction silently discards local edits made directly in the build tree
# -- that happened twice. BR2_LINUX_KERNEL_PATCH makes buildroot re-apply the
# patches after every extraction instead.
BR2_LINUX_KERNEL_PATCH="board/espressif/esp32s3/patches/linux board/espressif/esp32s3/tdeck-pro/patches/linux"
BR2_LINUX_KERNEL_USE_CUSTOM_CONFIG=y
BR2_LINUX_KERNEL_CUSTOM_CONFIG_FILE="board/espressif/esp32s3/tdeck_pro_linux.config"
BR2_LINUX_KERNEL_IMAGE_TARGET_CUSTOM=y
BR2_LINUX_KERNEL_IMAGE_TARGET_NAME="xipImage"
BR2_LINUX_KERNEL_NEEDS_HOST_OPENSSL=y
BR2_PACKAGE_LINUX_TOOLS_GPIO=y
BR2_PACKAGE_BUSYBOX_CONFIG="board/espressif/esp32s3/busybox.config"
BR2_PACKAGE_DROPBEAR=y
BR2_PACKAGE_IW=y
BR2_PACKAGE_WIRELESS_REGDB=y
BR2_PACKAGE_WPA_SUPPLICANT=y
# This board is WiFi client (STA) only. There is no AP mode: hostapd needs
# real fork() (os_unix.c), which this target's NOMMU uclibc-fdpic does not
# declare, and the firmware's own SoftAP wedged the wifi task so the STA could
# no longer scan -- so it was removed rather than shipped broken.
BR2_PACKAGE_IPTABLES=y
# The classic nano editor replaces busybox vi (disabled in busybox.config).
# Pulls in ncurses; EDITOR/VISUAL are set to nano in /etc/profile.d/editor.sh.
BR2_PACKAGE_NANO=y
# BR2_PACKAGE_URANDOM_SCRIPTS is not set
# Deliberately obvious "changeme" default (not a plausible-looking fake
# secure password) so it's unambiguous this MUST be changed via `passwd`
# before any real deployment. SSH/telnet no longer allow blank passwords.
BR2_TARGET_GENERIC_ROOT_PASSWD="changeme123"
BR2_TARGET_ROOTFS_CRAMFS=y
BR2_TARGET_ROOTFS_CRAMFS_XIP=y
BR2_TARGET_ROOTFS_JFFS2=y
BR2_TARGET_ROOTFS_JFFS2_FLASH_64=y
# BR2_TARGET_ROOTFS_TAR is not set
BR2_PACKAGE_LUA=y
BR2_PACKAGE_MBEDTLS=y
BR2_PACKAGE_LIBCURL=y
BR2_PACKAGE_LIBCURL_CURL=y
BR2_PACKAGE_LIBCURL_MBEDTLS=y
